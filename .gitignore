# Environment Variables (Sensitive data like API keys, passwords)
.env

# Virtual Environment (Dependencies are in requirements.txt, no need to upload this)
linkedln/

# Python cache files
__pycache__/
*.pyc

# IDE settings (Optional but recommended)
.vscode/
.DS_Store